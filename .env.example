# Server
PORT=3000

# Database
# Example: postgresql://user:password@localhost:5432/booking_db
DATABASE_URL=

# Auth
JWT_SECRET=
JWT_EXPIRES_IN=7d

# Email (SMTP or provider-specific credentials)
SMTP_USER=
SMTP_PASS=

# Stripe
STRIPE_PUBLISHABLE_KEY=
STRIPE_SECRET_KEY=

# Google Calendar (optional)
# Path to a service account JSON key file (if using Google Calendar API)
GOOGLE_SERVICE_ACCOUNT_KEY_PATH=./service-account-key.json
